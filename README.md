# SmartMerge

Deterministic offline semantic interference detection for four-tree git merges.

SmartMerge reports semantic interference before a merge is committed. Git still owns textual conflicts. This repository is the public source, documentation, build, and release surface for that tool.
